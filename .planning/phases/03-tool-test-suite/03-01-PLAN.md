---
phase: 03-tool-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_utils.py
autonomous: true
requirements: [TEST-01, TEST-04]
must_haves:
  truths:
    - "to_pascal_case is covered for multi-underscore, trailing-underscore, and digit-in-segment inputs"
    - "validate_name rejects hyphen, space, and double-underscore names with SystemExit(1)"
    - "The full test_utils.py suite passes with zero failures"
  artifacts:
    - path: "tests/test_utils.py"
      provides: "Validator and to_pascal_case edge-case unit tests (TEST-01, TEST-04)"
      contains: "def test_to_pascal_case_multiple_underscores"
  key_links:
    - from: "tests/test_utils.py"
      to: "fclean.to_pascal_case"
      via: "top-level package import"
      pattern: "from fclean import .*to_pascal_case"
    - from: "tests/test_utils.py"
      to: "fclean.validate_name"
      via: "pytest.raises(SystemExit)"
      pattern: "pytest\\.raises\\(SystemExit\\)"
---

<objective>
Close the TEST-01 and TEST-04 gaps by extending `tests/test_utils.py` with the missing
edge-case tests for `to_pascal_case()` and `validate_name()`.

Purpose: The existing suite covers happy-path and basic invalid cases only. The roadmap
requires coverage of `to_pascal_case` edge cases (multiple underscores, trailing underscore,
digit-at-end-of-segment) and additional `validate_name` rejection cases (hyphen, space,
double underscore). These are the regression guard for the Phase 1 PascalCase/validation fixes.
Output: an extended `tests/test_utils.py` with all new tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-test-suite/03-RESEARCH.md
@.planning/phases/03-tool-test-suite/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add to_pascal_case edge-case tests (TEST-01)</name>
  <files>tests/test_utils.py</files>
  <read_first>
    - tests/test_utils.py (current state: 8 tests; imports `from fclean import to_pascal_case, validate_name`; new tests append below the existing to_pascal_case block)
    - fclean/generators/validator.py (source of `to_pascal_case` — confirm actual output for trailing underscore before asserting)
  </read_first>
  <behavior>
    - test_to_pascal_case_multiple_underscores: to_pascal_case("my_long_feature_name") == "MyLongFeatureName"
    - test_to_pascal_case_trailing_underscore: to_pascal_case("auth_") == "Auth" (empty trailing segment, NOT "Auth_")
    - test_to_pascal_case_digit_end_segment: to_pascal_case("feature2_auth") == "Feature2Auth"
  </behavior>
  <action>
    Append three test functions to tests/test_utils.py, after the existing
    test_to_pascal_case_with_digit function. Use plain assert equality (no fixtures needed —
    to_pascal_case is a pure function). Add exactly these functions with these names and
    assertions: test_to_pascal_case_multiple_underscores asserting
    to_pascal_case("my_long_feature_name") == "MyLongFeatureName";
    test_to_pascal_case_trailing_underscore asserting to_pascal_case("auth_") == "Auth";
    test_to_pascal_case_digit_end_segment asserting to_pascal_case("feature2_auth") == "Feature2Auth".
    Before finalizing the trailing-underscore assertion, run the function in a python REPL
    (.venv/bin/python -c) to confirm the actual return value is "Auth" — per RESEARCH.md
    Pitfall 5, "auth_".split("_") yields ["auth",""] and "".capitalize() == "". If the actual
    output differs, assert the real value and add a one-line comment documenting it. Reuse the
    existing top-level import `from fclean import to_pascal_case, validate_name`; do not add a
    submodule import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_utils.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_utils.py contains `def test_to_pascal_case_multiple_underscores(`
    - tests/test_utils.py contains `def test_to_pascal_case_trailing_underscore(`
    - tests/test_utils.py contains `def test_to_pascal_case_digit_end_segment(`
    - `.venv/bin/pytest tests/test_utils.py -x -q` exits 0
    - `.venv/bin/pytest tests/test_utils.py --collect-only -q` lists the three new test node IDs
  </acceptance_criteria>
  <done>Three new to_pascal_case edge-case tests exist and pass; TEST-01 edge-case coverage complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validate_name rejection tests (TEST-04)</name>
  <files>tests/test_utils.py</files>
  <read_first>
    - tests/test_utils.py (current state after Task 1; existing SystemExit pattern at test_validate_name_invalid_exits, lines ~22-25)
    - fclean/generators/validator.py (confirm `_NAME_RE = re.compile(r"^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$")` rejects hyphen, space, double underscore)
  </read_first>
  <behavior>
    - test_validate_name_hyphen_exits: validate_name("my-feature") raises SystemExit with code == 1
    - test_validate_name_space_exits: validate_name("my feature") raises SystemExit with code == 1
    - test_validate_name_double_underscore_exits: validate_name("my__feature") raises SystemExit with code == 1
  </behavior>
  <action>
    Append three test functions to tests/test_utils.py, after the existing validate_name tests.
    Each follows the established SystemExit assertion pattern: wrap the call in
    `with pytest.raises(SystemExit) as exc_info:` and assert `exc_info.value.code == 1`.
    Add exactly: test_validate_name_hyphen_exits calling validate_name("my-feature");
    test_validate_name_space_exits calling validate_name("my feature");
    test_validate_name_double_underscore_exits calling validate_name("my__feature").
    These three inputs are all rejected by _NAME_RE per RESEARCH.md and 03-PATTERNS.md
    "Key Codebase Facts". Reuse the existing `import pytest` and top-level fclean import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_utils.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_utils.py contains `def test_validate_name_hyphen_exits(`
    - tests/test_utils.py contains `def test_validate_name_space_exits(`
    - tests/test_utils.py contains `def test_validate_name_double_underscore_exits(`
    - Each new test uses `pytest.raises(SystemExit)` and asserts `.value.code == 1`
    - `.venv/bin/pytest tests/test_utils.py -v` shows 0 failed and lists all three new tests as passed
  </acceptance_criteria>
  <done>Three new validate_name rejection tests exist and pass; TEST-04 edge-case coverage complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan adds unit tests for pure functions
(`to_pascal_case`) and an existing validator (`validate_name`). No filesystem writes,
no network calls, no new user-facing input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | dependency installs | accept | No new packages installed; pytest 9.0.3 is a pre-existing, approved dependency (RESEARCH.md Package Legitimacy Audit). No install task in this plan. |

No new threat surface introduced (test-only plan exercising the existing input-validation boundary).
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_utils.py -v` exits 0 with all tests passing
- New test count: 6 added (3 to_pascal_case + 3 validate_name)
</verification>

<success_criteria>
- TEST-01 edge cases (multiple underscores, trailing underscore, digit-at-end) are covered and passing
- TEST-04 edge cases (hyphen, space, double underscore) are covered and passing
- No existing test regressed
</success_criteria>

<output>
Create `.planning/phases/03-tool-test-suite/03-01-SUMMARY.md` when done
</output>
