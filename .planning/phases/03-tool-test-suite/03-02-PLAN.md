---
phase: 03-tool-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_generator.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "create_feature is tested for cubit, riverpod, getx, and None state types in an isolated temp dir"
    - "Each state-type test asserts the full expected file set, not just one file"
    - "The entity-format feature argument (feature:entity) is tested for entity + model file creation"
    - "The full test_generator.py suite passes with zero failures"
  artifacts:
    - path: "tests/test_generator.py"
      provides: "create_feature integration tests for every state branch (TEST-02)"
      contains: "def test_create_feature_cubit"
  key_links:
    - from: "tests/test_generator.py"
      to: "fclean.create_feature"
      via: "tmp_path + monkeypatch.chdir isolation"
      pattern: "monkeypatch\\.chdir\\(tmp_path\\)"
---

<objective>
Close the TEST-02 gap by extending `tests/test_generator.py` with integration tests that
exercise `create_feature()` for every `--state` branch (cubit, riverpod, getx, None) plus the
entity-format argument, each asserting the full expected file set in an isolated temp directory.

Purpose: The existing suite covers the bloc branch and the skip-existing behavior only. The
roadmap requires every state type to be verified for correct file generation. Without this, the
cubit/riverpod/getx/None code paths in `create_feature()` are untested.
Output: an extended `tests/test_generator.py` with all new tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-test-suite/03-RESEARCH.md
@.planning/phases/03-tool-test-suite/03-PATTERNS.md
@fclean/generators/feature.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cubit, riverpod, and getx generator tests (TEST-02)</name>
  <files>tests/test_generator.py</files>
  <read_first>
    - tests/test_generator.py (current state: imports `from fclean import get_bloc_templates, create_feature`; established tmp_path+monkeypatch pattern at test_create_feature_creates_expected_files)
    - fclean/generators/feature.py (confirm base file paths and per-state subdirectories actually written)
    - .planning/phases/03-tool-test-suite/03-RESEARCH.md (Code Examples section: BLOC_FILES/CUBIT_FILES/RIVERPOD_FILES/GETX_FILES exact path lists)
    - .planning/phases/03-tool-test-suite/03-PATTERNS.md ("Key Codebase Facts": template return key structure per state type)
  </read_first>
  <behavior>
    - test_create_feature_cubit: after create_feature("auth","cubit"), files exist at the 4 base paths plus presentation/cubit/auth_state.dart and presentation/cubit/auth_cubit.dart
    - test_create_feature_riverpod: after create_feature("auth","riverpod"), domain/repository/auth_repository.dart and presentation/providers/auth_provider.dart exist
    - test_create_feature_getx: after create_feature("auth","getx"), presentation/controller/auth_controller.dart and presentation/bindings/auth_binding.dart exist
  </behavior>
  <action>
    Append three test functions to tests/test_generator.py. Each takes (tmp_path, monkeypatch),
    calls monkeypatch.chdir(tmp_path) FIRST (mandatory per RESEARCH.md Pitfall 1 — without it
    files land in the repo root), then calls create_feature with the state type, then asserts
    the full file set with `(tmp_path / "<relative path>").exists()`.
    test_create_feature_cubit asserts all four base files
    (data/datasources/auth_remote_datasource.dart, data/datasources/auth_local_datasource.dart,
    data/repository/auth_repository_impl.dart, domain/repository/auth_repository.dart) plus
    presentation/cubit/auth_state.dart and presentation/cubit/auth_cubit.dart, all under
    lib/features/auth/.
    test_create_feature_riverpod asserts lib/features/auth/domain/repository/auth_repository.dart
    and lib/features/auth/presentation/providers/auth_provider.dart.
    test_create_feature_getx asserts lib/features/auth/domain/repository/auth_repository.dart,
    lib/features/auth/presentation/controller/auth_controller.dart, and
    lib/features/auth/presentation/bindings/auth_binding.dart.
    Use exact subdirectory names from 03-PATTERNS.md: cubit→presentation/cubit/,
    riverpod→presentation/providers/, getx→presentation/controller/ + presentation/bindings/.
    Reuse the existing top-level `from fclean import ... create_feature` import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_generator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_generator.py contains `def test_create_feature_cubit(tmp_path, monkeypatch):`
    - tests/test_generator.py contains `def test_create_feature_riverpod(tmp_path, monkeypatch):`
    - tests/test_generator.py contains `def test_create_feature_getx(tmp_path, monkeypatch):`
    - Each new test calls `monkeypatch.chdir(tmp_path)` before `create_feature(`
    - test_create_feature_cubit asserts presentation/cubit/auth_cubit.dart exists (not presentation/bloc/)
    - `.venv/bin/pytest tests/test_generator.py -x -q` exits 0
    - `git status --porcelain lib/` produces no output after the test run (no leakage into repo root)
  </acceptance_criteria>
  <done>cubit, riverpod, and getx state branches of create_feature are verified with full file-set assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add None-state and entity-path generator tests (TEST-02)</name>
  <files>tests/test_generator.py</files>
  <read_first>
    - tests/test_generator.py (current state after Task 1)
    - fclean/generators/feature.py (confirm None state writes no state subdirectory; entity_name path writes domain/entities/<e>.dart and data/models/<e>_model.dart)
    - .planning/phases/03-tool-test-suite/03-RESEARCH.md (ENTITY_EXTRA_FILES list)
  </read_first>
  <behavior>
    - test_create_feature_no_state: after create_feature("auth", None), base repository file exists but NO presentation/bloc, presentation/cubit, presentation/providers, or presentation/controller directory exists
    - test_create_feature_with_entity: after create_feature("auth:user", "bloc"), domain/entities/user.dart and data/models/user_model.dart exist
  </behavior>
  <action>
    Append two test functions to tests/test_generator.py, each taking (tmp_path, monkeypatch)
    and calling monkeypatch.chdir(tmp_path) first.
    test_create_feature_no_state calls create_feature("auth", None), asserts
    lib/features/auth/domain/repository/auth_repository.dart EXISTS, and asserts
    `not (tmp_path / "lib/features/auth/presentation/bloc").exists()` plus the same negative
    assertion for presentation/cubit, presentation/providers, and presentation/controller.
    test_create_feature_with_entity calls create_feature("auth:user", "bloc") and asserts
    lib/features/auth/domain/entities/user.dart and lib/features/auth/data/models/user_model.dart
    both exist. Pass None as a Python literal (not the string "None"). Reuse the existing import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_generator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_generator.py contains `def test_create_feature_no_state(tmp_path, monkeypatch):`
    - tests/test_generator.py contains `def test_create_feature_with_entity(tmp_path, monkeypatch):`
    - test_create_feature_no_state contains `create_feature("auth", None)` (None literal, not "None")
    - test_create_feature_no_state contains at least one `assert not (` negative directory assertion
    - test_create_feature_with_entity asserts user.dart and user_model.dart exist
    - `.venv/bin/pytest tests/test_generator.py -v` shows 0 failed
  </acceptance_criteria>
  <done>None-state (no state files) and entity-format (entity + model files) branches are verified; TEST-02 coverage complete across all branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan adds integration tests that call
`create_feature()` against the `tmp_path` fixture. All filesystem writes are confined to
pytest's per-test temp directory; `monkeypatch.chdir(tmp_path)` guarantees no writes escape
to the repository root. No network calls, no new user-facing input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | test filesystem writes | mitigate | Every create_feature() call is preceded by monkeypatch.chdir(tmp_path); acceptance criterion asserts `git status --porcelain lib/` is empty so writes cannot pollute the repo. |
| T-03-SC | Tampering | dependency installs | accept | No new packages installed; pytest is pre-existing and approved (RESEARCH.md audit). No install task in this plan. |

No new threat surface introduced beyond confined temp-directory writes (test-only plan).
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_generator.py -v` exits 0 with all tests passing
- `git status --porcelain lib/` is empty after running the suite (no temp-dir leakage)
- New test count: 5 added (cubit, riverpod, getx, None, entity)
</verification>

<success_criteria>
- TEST-02 covers create_feature for bloc (existing), cubit, riverpod, getx, and None states
- The entity-format argument path is verified
- Every test asserts the full expected file set, not a single file
- No filesystem writes escape the temp directory
</success_criteria>

<output>
Create `.planning/phases/03-tool-test-suite/03-02-SUMMARY.md` when done
</output>
