---
phase: 02-restructure-package
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pyproject.toml
  - tests/test_utils.py
  - tests/test_generator.py
  - tests/test_templates.py
autonomous: true
requirements:
  - PKG-02
  - PKG-03
user_setup: []

must_haves:
  truths:
    - "pip install -e . (or pip3 install -e \".[dev]\") succeeds and puts the fclean console command on PATH"
    - "which fclean returns a path in the Python bin and fclean --help shows the argparse help"
    - "The three test files no longer contain sys.path.insert(0, \".\") and import via from fclean import ... unchanged"
    - "python3 -m pytest passes with all 12 existing tests green after restructure (no regression — PKG-03)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build metadata, requires-python >=3.8, and the fclean console-script entry point"
      contains: "[project.scripts]"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "fclean/cli.py main()"
      via: "entry point declaration fclean = \"fclean.cli:main\""
      pattern: "fclean = \"fclean.cli:main\""
    - from: "tests/*.py"
      to: "fclean package (editable install)"
      via: "from fclean import ... resolved via pip install -e ."
      pattern: "from fclean import"
---

<objective>
Add a `pyproject.toml` declaring the hatchling build backend, project metadata (`requires-python = ">=3.8"`), and the `fclean = "fclean.cli:main"` console-script entry point. Install the package editably, then remove the now-obsolete `sys.path.insert(0, ".")` hacks from the three test files. Confirm the full 12-test suite stays green and the `fclean` CLI works identically to before (PKG-03 regression gate).

Purpose: Makes fclean a proper installable Python package (PKG-02) so `pip install .` exposes `fclean` as a global CLI command, and proves the restructure introduced no behavioral regression (PKG-03).
Output: `pyproject.toml` at repo root; three test files with the `sys.path` hack removed; an editable install on PATH; green pytest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-restructure-package/02-RESEARCH.md
@.planning/phases/02-restructure-package/02-PATTERNS.md
@.planning/phases/02-restructure-package/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and install editably</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/02-restructure-package/02-RESEARCH.md (Pattern 2 + Code Examples: complete pyproject.toml; Package Legitimacy Audit approving hatchling; Pitfall 4 on hatchling auto-fetch)
    - .planning/phases/02-restructure-package/02-01-SUMMARY.md (confirms fclean.cli:main entry-point target exists)
    - fclean/cli.py (confirms main() is the entry point)
  </read_first>
  <action>
    Create `pyproject.toml` at the repo root with these exact tables and values:

    `[build-system]` — `requires = ["hatchling >= 1.26"]`, `build-backend = "hatchling.build"`. Hatchling is the PyPA-endorsed backend (approved [OK] in RESEARCH.md Package Legitimacy Audit, github.com/pypa/hatch). Modern pip auto-fetches it in an isolated build environment, so no manual pre-install is needed (RESEARCH.md Pitfall 4).

    `[project]` — `name = "fclean"`, `version = "0.1.0"`, `description = "Flutter Clean Architecture feature scaffold generator"`, `readme = "README.md"`, `requires-python = ">=3.8"`.

    `[project.scripts]` — `fclean = "fclean.cli:main"` (targets the main() created in Plan 01).

    `[project.optional-dependencies]` — `dev = ["pytest>=7"]`. Do NOT add `pytest-tmp-path` — that package does not exist on PyPI; `tmp_path` is a built-in pytest fixture (RESEARCH.md Package Legitimacy Audit). Adding it would cause pip install to fail.

    Do NOT create `setup.py` or `MANIFEST.in` — hatchling reads all metadata from pyproject.toml and respects `.gitignore` for file discovery.

    After writing the file, run `pip3 install -e ".[dev]"` to install editably. This is the step that makes `from fclean import ...` resolve in tests without any sys.path hack and puts the `fclean` command on PATH.
  </action>
  <verify>
    <automated>pip3 install -e ".[dev]" >/dev/null 2>&1 && which fclean && fclean --help >/dev/null 2>&1 && echo INSTALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `[build-system]`, `requires = ["hatchling >= 1.26"]`, and `build-backend = "hatchling.build"`
    - `pyproject.toml` contains `[project]` with `requires-python = ">=3.8"` and `version = "0.1.0"`
    - `pyproject.toml` contains `[project.scripts]` with the line `fclean = "fclean.cli:main"`
    - `pyproject.toml` does NOT contain the string `pytest-tmp-path` and does NOT reference setup.py
    - `pip3 install -e ".[dev]"` exits 0
    - `which fclean` returns a path inside the active Python environment's bin directory
    - `fclean --help` exits 0 and prints argparse usage including `--features` and `--state`
  </acceptance_criteria>
  <done>pyproject.toml declares hatchling, metadata, and the fclean entry point; `pip3 install -e ".[dev]"` succeeds; the `fclean` command is on PATH and `fclean --help` works.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sys.path hacks from tests and verify no regression</name>
  <files>tests/test_utils.py, tests/test_generator.py, tests/test_templates.py</files>
  <read_first>
    - tests/test_utils.py (lines 1-4: import block with sys.path.insert hack)
    - tests/test_generator.py (lines 1-3: import block)
    - tests/test_templates.py (lines 1-3: import block)
    - .planning/phases/02-restructure-package/02-RESEARCH.md (Pattern 3 + Pitfall 2: remove sys.path.insert only after editable install resolves imports)
  </read_first>
  <action>
    In all three test files, remove the `sys.path.insert(0, ".")` line. Also remove the now-unused `import sys` line where `sys` is used only for that path insert (test_generator.py and test_templates.py: `import sys` becomes unused — delete it; test_utils.py: keep `import pytest`, delete `import sys` and the path-insert line). The `from fclean import ...` import lines stay byte-for-byte identical — the editable install from Task 1 makes them resolve. You may also drop the trailing `# NOTE: import path changes after Phase 2 package restructure` comment since the restructure is now done, but this is optional and not required for correctness.

    Do not change any test logic, assertions, or fixtures. The only change is the import header.

    Then run the full suite. PKG-03 requires all 12 existing tests to remain green after the restructure — this is the regression gate. Also run the CLI regression check from a Flutter-project-like temp dir is not required here (tests already cover create_feature via monkeypatch.chdir), but confirm `fclean --features auth --state bloc` does not crash on argparse by relying on the test suite's generator coverage.
  </action>
  <verify>
    <automated>! grep -rl 'sys.path.insert' tests/test_utils.py tests/test_generator.py tests/test_templates.py && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - None of `tests/test_utils.py`, `tests/test_generator.py`, `tests/test_templates.py` contains the string `sys.path.insert` (grep -c returns 0 for each)
    - Each test file still contains its original `from fclean import ...` line unchanged (test_utils: `from fclean import to_pascal_case, validate_name`; test_generator: `from fclean import get_bloc_templates, create_feature`; test_templates: `from fclean import get_riverpod_templates`)
    - No test file imports `sys` unless `sys` is still used elsewhere in that file
    - `python3 -m pytest -q` exits 0 with 12 passed (PKG-03 — no regression)
    - `python3 -m pytest -v` reports the same test names as before the restructure (no tests dropped or renamed)
  </acceptance_criteria>
  <done>The three test files import the package cleanly with no sys.path manipulation, and the full 12-test suite passes against the new package layout, proving no behavioral regression (PKG-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip build environment → local install | `pip3 install -e ".[dev]"` fetches the hatchling build backend from PyPI into an isolated build env. hatchling is PyPA first-party, approved [OK] in RESEARCH.md Package Legitimacy Audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | Supply chain: hatchling (build backend) + pytest (dev dep) installed via pip | accept | Both packages are PyPA/pytest-dev first-party, verified on PyPI with multi-year history and very-high download counts, slopcheck [OK] (RESEARCH.md Package Legitimacy Audit). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking-human checkpoint required per planner package-legitimacy gate. pytest-tmp-path (a non-existent package) is explicitly excluded. |
| T-02-03 | Tampering | pyproject.toml entry-point wiring (`fclean.cli:main`) | mitigate | Acceptance-criteria grep gate asserts the exact `fclean = "fclean.cli:main"` line and `which fclean` + `fclean --help` confirm the entry point resolves to the real main(). |

**Security assessment:** Pure packaging phase. No network calls at runtime, no auth, no crypto, no external data beyond CLI args validated in Phase 1. The only supply-chain surface is the two approved, audited build/dev dependencies (hatchling, pytest), both rated [OK] with no [ASSUMED]/[SUS]/[SLOP] verdicts — so no high-severity threat and no legitimacy checkpoint is triggered. High-severity block condition: not triggered.
</threat_model>

<verification>
- `pip3 install -e ".[dev]"` exits 0 in the environment (PKG-02)
- `which fclean` returns a Python-bin path; `fclean --help` exits 0 (PKG-02 entry point on PATH)
- `pyproject.toml` contains `[project.scripts]` line `fclean = "fclean.cli:main"`
- No test file contains `sys.path.insert`
- `python3 -m pytest -q` reports 12 passed (PKG-03 regression gate)
</verification>

<success_criteria>
- `pip install -e .` succeeds in a clean environment and exposes `fclean` on PATH (PKG-02)
- `fclean --features auth --state bloc` works identically to before via the new entry point (PKG-03)
- All 12 existing tests pass with no sys.path hacks (PKG-03)
- No `setup.py`, `MANIFEST.in`, or non-existent `pytest-tmp-path` is introduced
</success_criteria>

<output>
Create `.planning/phases/02-restructure-package/02-02-SUMMARY.md` when done.
</output>
