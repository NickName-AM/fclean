---
phase: 02-restructure-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fclean/__init__.py
  - fclean/cli.py
  - fclean/generators/__init__.py
  - fclean/generators/feature.py
  - fclean/generators/validator.py
  - fclean/templates/__init__.py
  - fclean/templates/bloc.py
  - fclean/templates/cubit.py
  - fclean/templates/riverpod.py
  - fclean/templates/getx.py
  - fclean.py
autonomous: true
requirements:
  - PKG-01
user_setup: []

must_haves:
  truths:
    - "Public symbols import from fclean.* submodules (from fclean.cli import main; from fclean.generators.feature import create_feature; from fclean.generators.validator import to_pascal_case)"
    - "The root fclean.py file no longer exists"
    - "from fclean import to_pascal_case, validate_name, create_feature, get_bloc_templates, get_cubit_templates, get_riverpod_templates, get_getx_templates resolves via the package __init__"
    - "Generator logic produces identical Dart output to the pre-restructure single file"
  artifacts:
    - path: "fclean/__init__.py"
      provides: "Public API re-export layer"
      contains: "from fclean.generators.validator import"
    - path: "fclean/cli.py"
      provides: "argparse main() entry point"
      contains: "def main("
    - path: "fclean/generators/feature.py"
      provides: "create_feature() and is_flutter_project()"
      contains: "def create_feature("
    - path: "fclean/generators/validator.py"
      provides: "to_pascal_case() and validate_name()"
      contains: "def to_pascal_case("
    - path: "fclean/templates/bloc.py"
      provides: "get_bloc_templates()"
      contains: "def get_bloc_templates("
    - path: "fclean/templates/cubit.py"
      provides: "get_cubit_templates()"
      contains: "def get_cubit_templates("
    - path: "fclean/templates/riverpod.py"
      provides: "get_riverpod_templates()"
      contains: "def get_riverpod_templates("
    - path: "fclean/templates/getx.py"
      provides: "get_getx_templates()"
      contains: "def get_getx_templates("
    - path: "fclean/generators/__init__.py"
      provides: "subpackage marker for hatchling discovery"
    - path: "fclean/templates/__init__.py"
      provides: "subpackage marker for hatchling discovery"
  key_links:
    - from: "fclean/generators/feature.py"
      to: "fclean/templates/*.py"
      via: "direct submodule imports of get_*_templates"
      pattern: "from fclean.templates.bloc import get_bloc_templates"
    - from: "fclean/templates/bloc.py"
      to: "fclean/generators/validator.py"
      via: "direct submodule import of to_pascal_case"
      pattern: "from fclean.generators.validator import to_pascal_case"
    - from: "fclean/__init__.py"
      to: "fclean/generators/*.py and fclean/templates/*.py"
      via: "re-export imports (never circular from fclean import ...)"
      pattern: "from fclean.generators.feature import create_feature"
---

<objective>
Split the 199-line single-file `fclean.py` into a proper Python package directory tree (`fclean/`), distributing code across submodules per the roadmap layout, with `fclean/__init__.py` re-exporting the existing public API so the three test files' `from fclean import ...` statements remain valid. Delete `fclean.py` in the same plan so the package directory and the module file never coexist.

Purpose: Establishes the module structure (PKG-01) required before any further features are layered in. A 199-line single file would accrue maintenance debt as Phases 4-5 add UseCase, Provider, and test scaffolding.
Output: The `fclean/` package with `__init__.py`, `cli.py`, `generators/` (feature.py, validator.py), and `templates/` (bloc.py, cubit.py, riverpod.py, getx.py), plus empty subpackage `__init__.py` markers. The root `fclean.py` is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-restructure-package/02-RESEARCH.md
@.planning/phases/02-restructure-package/02-PATTERNS.md
@fclean.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create leaf submodules — validator and the four template providers</name>
  <files>fclean/generators/__init__.py, fclean/generators/validator.py, fclean/templates/__init__.py, fclean/templates/bloc.py, fclean/templates/cubit.py, fclean/templates/riverpod.py, fclean/templates/getx.py</files>
  <read_first>
    - fclean.py (current single-file source — lines 7-29 for validator, 36-104 for the four template providers)
    - .planning/phases/02-restructure-package/02-PATTERNS.md (verbatim extraction targets and import-discipline rules)
  </read_first>
  <action>
    Create the dependency-graph leaves first so later modules can import them.

    Create `fclean/generators/__init__.py` and `fclean/templates/__init__.py` as zero-byte empty files (required so hatchling auto-discovery includes both subdirectories in the wheel; without them Python 3 treats the directories as namespace packages and hatchling's package scan breaks).

    Create `fclean/generators/validator.py`: move `to_pascal_case` (fclean.py lines 7-15), the `_NAME_RE = re.compile(r"^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$")` module constant (line 18), and `validate_name` (lines 21-29) verbatim. Top imports: only `import sys` and `import re`. This module has NO imports from other fclean submodules — it is the leaf of the dependency graph. Keep the existing stderr + sys.exit(1) error pattern unchanged.

    Create `fclean/templates/bloc.py`, `cubit.py`, `riverpod.py`, `getx.py`: move `get_bloc_templates` (lines 36-55), `get_cubit_templates` (lines 57-70), `get_riverpod_templates` (lines 72-86), `get_getx_templates` (lines 88-104) verbatim — one function per file. Each template file's only import is `from fclean.generators.validator import to_pascal_case`. Do NOT write `from fclean import to_pascal_case` — that routes through `__init__.py` and causes a circular import (ImportError: cannot import name from partially initialized module).
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; [ast.parse(open(f).read()) for f in ['fclean/generators/validator.py','fclean/templates/bloc.py','fclean/templates/cubit.py','fclean/templates/riverpod.py','fclean/templates/getx.py']]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `fclean/generators/__init__.py` exists and is zero bytes (`test ! -s fclean/generators/__init__.py` succeeds)
    - `fclean/templates/__init__.py` exists and is zero bytes
    - `fclean/generators/validator.py` contains `def to_pascal_case(` and `def validate_name(` and the regex `^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$`
    - `fclean/generators/validator.py` does NOT contain `from fclean` (grep -c 'from fclean' returns 0)
    - Each of bloc.py, cubit.py, riverpod.py, getx.py contains exactly its one `def get_*_templates(` function and the line `from fclean.generators.validator import to_pascal_case`
    - No template file contains `from fclean import` (grep -L confirms direct submodule import discipline)
  </acceptance_criteria>
  <done>The validator and four template provider modules exist with verbatim logic, correct direct-submodule imports, and both subpackage markers are empty files. All five non-empty modules parse as valid Python.</done>
</task>

<task type="auto">
  <name>Task 2: Create feature.py, cli.py, __init__.py and delete fclean.py</name>
  <files>fclean/generators/feature.py, fclean/cli.py, fclean/__init__.py, fclean.py</files>
  <read_first>
    - fclean.py (lines 32-34 is_flutter_project, 106-179 create_feature, 181-199 main)
    - fclean/generators/validator.py (created in Task 1 — confirms import target symbols)
    - fclean/templates/bloc.py (created in Task 1 — confirms get_bloc_templates signature)
    - .planning/phases/02-restructure-package/02-PATTERNS.md (re-export surface and atomic-delete instruction)
    - .planning/phases/02-restructure-package/02-RESEARCH.md (Pitfall 1: fclean.py and fclean/ must not coexist)
  </read_first>
  <action>
    Create `fclean/generators/feature.py`: move `is_flutter_project` (fclean.py lines 32-34) and `create_feature` (lines 106-179) verbatim. Top imports: `import sys`, `from pathlib import Path`, then direct submodule imports `from fclean.generators.validator import validate_name, to_pascal_case` and `from fclean.templates.bloc import get_bloc_templates`, `from fclean.templates.cubit import get_cubit_templates`, `from fclean.templates.riverpod import get_riverpod_templates`, `from fclean.templates.getx import get_getx_templates`. Keep `path.write_text(content, encoding="utf-8")` with the explicit `encoding="utf-8"` (prior bug fix WR-02 — do not omit). Keep the WR-03 unknown-state-type guard and the WR-01 single-colon guard verbatim.

    Create `fclean/cli.py`: move `main()` (fclean.py lines 181-199) verbatim including the `if __name__ == "__main__": main()` block. Top imports: `import sys`, `import argparse`, `from fclean.generators.feature import create_feature, is_flutter_project`. This is the `[project.scripts]` entry-point target (`fclean.cli:main`) declared in Plan 02.

    Create `fclean/__init__.py` re-exporting the public API the three test files import. Use direct submodule paths: `from fclean.generators.validator import to_pascal_case, validate_name`, `from fclean.generators.feature import create_feature`, and `from fclean.templates.{bloc,cubit,riverpod,getx} import get_{...}_templates`. Define `__all__` listing all seven symbols: to_pascal_case, validate_name, create_feature, get_bloc_templates, get_cubit_templates, get_riverpod_templates, get_getx_templates.

    Delete `fclean.py` in this same task (atomic with package creation). Python import resolution prefers the directory, but leaving the file causes IDE confusion and edge-case import conflicts. Verify deletion with `python3 -c "import fclean; print(fclean.__file__)"` printing a path ending in `fclean/__init__.py`, not `fclean.py`.
  </action>
  <verify>
    <automated>rm -f fclean.py && python3 -c "import fclean; assert fclean.__file__.endswith('fclean/__init__.py'), fclean.__file__; from fclean.cli import main; from fclean.generators.feature import create_feature, is_flutter_project; from fclean.generators.validator import to_pascal_case, validate_name; from fclean import to_pascal_case, validate_name, create_feature, get_bloc_templates, get_cubit_templates, get_riverpod_templates, get_getx_templates; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `fclean.py` no longer exists at repo root (`test ! -e fclean.py` succeeds)
    - `python3 -c "import fclean; print(fclean.__file__)"` prints a path ending in `fclean/__init__.py`
    - `fclean/generators/feature.py` contains `def create_feature(`, `def is_flutter_project(`, and `encoding="utf-8"`
    - `fclean/generators/feature.py` contains `from fclean.templates.bloc import get_bloc_templates` (direct submodule import, not `from fclean import`)
    - `fclean/cli.py` contains `def main(`, `import argparse`, and `from fclean.generators.feature import create_feature, is_flutter_project`
    - `fclean/__init__.py` contains `__all__` with all seven symbols and uses only `from fclean.generators.*` / `from fclean.templates.*` import paths (grep -c 'from fclean import' returns 0)
    - The smoke import command in <verify> exits 0
  </acceptance_criteria>
  <done>The full package is importable, `from fclean import ...` resolves all seven public symbols via __init__, submodule imports resolve, no circular import occurs, and the root `fclean.py` is deleted so the package is the sole `fclean` resolution target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → generator | Feature/entity names from argv reach filesystem path construction. Already validated in Phase 1 (`validate_name` regex + single-colon guard WR-01). This plan moves that logic verbatim; no new boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Path construction in `create_feature()` (argv → `Path("lib/features")/name`) | accept | Pre-existing input validation (`validate_name` regex `^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$` blocks `..`, `/`, absolute paths) is moved verbatim from Phase 1; this restructure changes no validation behavior. No high-severity threat. |
| T-02-02 | Tampering | Submodule import wiring (circular-import / wrong-module resolution) | mitigate | Enforce direct-submodule import discipline (`from fclean.generators.validator import ...`, never `from fclean import ...` inside the package) via acceptance-criteria grep gates in both tasks. Verified by smoke-import command. |

**Security assessment:** This is a pure Python packaging/restructure phase. It makes no network calls, handles no authentication, performs no cryptography, and processes no external data beyond CLI arguments already validated in Phase 1. No high-severity (network/auth/crypto/injection) threats are applicable. No npm/pip/cargo install tasks occur in this plan (installs are in Plan 02 against the audited hatchling backend per RESEARCH.md Package Legitimacy Audit). High-severity block condition: not triggered.
</threat_model>

<verification>
- `python3 -c "import fclean; print(fclean.__file__)"` prints `.../fclean/__init__.py` (not `fclean.py`)
- `python3 -c "from fclean.cli import main; from fclean.generators.feature import create_feature; from fclean.generators.validator import to_pascal_case; print('OK')"` prints OK (PKG-01 smoke)
- `python3 -c "from fclean import to_pascal_case, validate_name, create_feature, get_bloc_templates, get_cubit_templates, get_riverpod_templates, get_getx_templates; print('OK')"` prints OK (re-export surface intact)
- `test ! -e fclean.py` succeeds (root module removed)
- No file inside `fclean/` contains `from fclean import` (import discipline; prevents circular imports)
</verification>

<success_criteria>
- All logic from the former `fclean.py` lives in `fclean/` submodules per the roadmap layout (PKG-01)
- `from fclean import ...` resolves the seven public symbols via the re-export `__init__.py`
- Direct-submodule imports resolve with no circular import
- The root `fclean.py` is deleted; `import fclean` resolves to the package directory
</success_criteria>

<output>
Create `.planning/phases/02-restructure-package/02-01-SUMMARY.md` when done.
</output>
