---
phase: 01-foundation-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - fclean.py
  - tests/test_utils.py
autonomous: true
requirements:
  - CORE-02
  - DX-02
user_setup: []

must_haves:
  truths:
    - "Running fclean with an invalid feature or entity name prints a clear error to stderr and exits 1 without creating any directories or files"
    - "Valid names matching ^[a-z][a-z0-9_]*$ pass validation and scaffolding proceeds"
    - "Both the feature name and the entity name (the part after ':') are validated"
    - "When --state is omitted, fclean prints the explicit DX-02 notice once per invocation"
  artifacts:
    - path: "fclean.py"
      provides: "import re, _NAME_RE, validate_name(), validation calls in create_feature, DX-02 notice in main"
      contains: "def validate_name"
    - path: "tests/test_utils.py"
      provides: "validate_name unit tests (valid pass, invalid SystemExit)"
      contains: "def test_validate_name"
  key_links:
    - from: "create_feature"
      to: "validate_name"
      via: "called on feature_name and entity_name before any mkdir"
      pattern: "validate_name\\("
    - from: "main"
      to: "DX-02 notice"
      via: "args.state is None branch printing the notice string"
      pattern: "No state management files generated"
---

<objective>
Fix CORE-02 and DX-02. CORE-02: add `validate_name(name)` using `re.fullmatch(r"^[a-z][a-z0-9_]*$", name)` that prints a clear error to stderr and `sys.exit(1)` on failure; call it on both the feature name and the entity name in `create_feature()` BEFORE any filesystem write. DX-02: print the exact notice `No state management files generated. Pass --state <lib> to scaffold a state layer.` once in `main()` when `--state` is omitted.

Purpose: Without validation, a name like `../evil` writes directories outside the Flutter project tree (path traversal). Validation must precede every `mkdir`. The DX-02 notice replaces the current silent `(State: None)` behavior with an explicit, actionable message.
Output: `fclean.py` with `import re`, `_NAME_RE`, `validate_name()`, two validation calls in `create_feature()`, and the DX-02 notice in `main()`; validate_name unit tests added to `tests/test_utils.py`.

Depends on 01-01 because it edits the same file (fclean.py) and reuses the module-level helper placement established there.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md
@.planning/phases/01-foundation-fixes/01-PATTERNS.md
@.planning/phases/01-foundation-fixes/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validate_name, wire it into create_feature, and add the DX-02 notice</name>
  <files>fclean.py</files>
  <read_first>
    - fclean.py (current state AFTER 01-01 — import block, the to_pascal_case helper added by 01-01, create_feature name-parsing block around lines 71-74, the is_flutter_project guard in main around lines 134-136, the feature loop around line 138)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (Pattern 2: Input Validator; Pattern 3: State-Omission Notice; Pitfall 2: Validation After Directory Creation; Pitfall 3: Entity Name Not Validated; Pitfall 4: Missing import re)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (validate_name + DX-02 notice section — exact insertion points; the guard-then-exit shared pattern)
    - .planning/phases/01-foundation-fixes/01-01-SUMMARY.md (confirm where to_pascal_case was placed so new helpers sit beside it)
  </read_first>
  <behavior>
    - validate_name("auth") returns without raising
    - validate_name("user_profile") returns without raising
    - validate_name("../evil") raises SystemExit with code 1
    - validate_name("User") raises SystemExit (uppercase rejected)
    - validate_name("1auth") raises SystemExit (leading digit rejected)
    - validate_name("") raises SystemExit (empty rejected)
    - main() with args.state is None prints exactly "No state management files generated. Pass --state <lib> to scaffold a state layer."
  </behavior>
  <action>
    Add `import re` to the import block. Per 01-PATTERNS.md the block becomes `import sys` / `import re` / `import argparse` / `from pathlib import Path` (re inserted after sys).

    Immediately after the `to_pascal_case` helper (added by 01-01) and before `is_flutter_project()`, define a module-level compiled regex `_NAME_RE = re.compile(r"^[a-z][a-z0-9_]*$")`, then define `validate_name(name: str) -> None`. Its body: if `not _NAME_RE.fullmatch(name)`, print `Error: Invalid name '{name}'. Names must start with a lowercase letter and contain only [a-z0-9_].` to `sys.stderr` (use `file=sys.stderr`), then `sys.exit(1)`. This copies the existing guard-then-exit shape used by the is_flutter_project check. Use `fullmatch` (not `match`) so partial matches like `../evil` are rejected.

    In `create_feature()`, immediately after the `entity_name = parts[1] if len(parts) > 1 else None` line and BEFORE `base_path = Path("lib/features") / feature_name`, add `validate_name(feature_name)` and, guarded by `if entity_name is not None:`, `validate_name(entity_name)`. This placement is mandatory: validation must run before every `mkdir`.

    In `main()`, after the `is_flutter_project()` guard block and before `for feature in args.features:`, add `if args.state is None:` printing exactly `No state management files generated. Pass --state <lib> to scaffold a state layer.` (to stdout, single print, once per invocation — do NOT put this inside create_feature or the feature loop).
  </action>
  <verify>
    <automated>grep -q '^import re' fclean.py && grep -q 'def validate_name' fclean.py && grep -q 'No state management files generated' fclean.py && python3 -c "import sys; sys.path.insert(0,'.'); from fclean import validate_name; \nimport pytest" 2>/dev/null; echo checked</automated>
  </verify>
  <acceptance_criteria>
    - fclean.py contains `import re`
    - fclean.py contains `def validate_name(`
    - fclean.py contains `_NAME_RE = re.compile(r"^[a-z][a-z0-9_]*$")`
    - fclean.py contains the literal string `No state management files generated. Pass --state <lib> to scaffold a state layer.`
    - The two `validate_name(` calls in create_feature appear textually before the `base_path = Path(` assignment (verify by reading the function)
    - Behavior: `python3 -c "import sys; sys.path.insert(0,'.'); from fclean import validate_name; validate_name('auth')"` exits 0
    - Behavior: `python3 -c "import sys; sys.path.insert(0,'.'); from fclean import validate_name; validate_name('../evil')"` exits 1 and prints the error to stderr
  </acceptance_criteria>
  <done>import re added; _NAME_RE and validate_name defined at module level; both feature and entity names validated before any mkdir; DX-02 notice printed once in main when --state is None.</done>
</task>

<task type="auto">
  <name>Task 2: Add validate_name unit tests to tests/test_utils.py</name>
  <files>tests/test_utils.py</files>
  <read_first>
    - tests/test_utils.py (current state — to_pascal_case tests added by 01-01; preserve them and the sys.path.insert import header)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (Wave 0 stub for tests/test_utils.py — exact validate_name test bodies; the pytest SystemExit assertion shared pattern)
  </read_first>
  <action>
    Append validate_name tests to the existing `tests/test_utils.py` (do not remove the to_pascal_case tests). Ensure `import pytest` and `validate_name` are imported (extend the existing `from fclean import ...` line to include `validate_name`).

    Add: `test_validate_name_valid_passes` calls `validate_name("auth")` and `validate_name("user_profile")` and asserts neither raises. `test_validate_name_invalid_exits` uses `with pytest.raises(SystemExit) as exc_info: validate_name("../evil")` then asserts `exc_info.value.code == 1`. `test_validate_name_uppercase_exits` asserts `validate_name("User")` raises SystemExit. `test_validate_name_leading_digit_exits` asserts `validate_name("1auth")` raises SystemExit. `test_validate_name_empty_exits` asserts `validate_name("")` raises SystemExit.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_utils.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_utils.py contains `def test_validate_name_invalid_exits`
    - tests/test_utils.py still contains `def test_to_pascal_case_snake` (01-01 tests preserved)
    - `python3 -m pytest tests/test_utils.py -x -q` exits 0
  </acceptance_criteria>
  <done>validate_name tests added alongside existing to_pascal_case tests; full test_utils.py passes including the SystemExit code-1 assertion for path-traversal input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg -> filesystem path | User-supplied feature/entity name becomes a directory path under lib/features and a Dart file/class name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | feature/entity name -> path under lib/features (path traversal via `../evil`) | mitigate | validate_name rejects any name not matching `^[a-z][a-z0-9_]*$` (no `/`, `.`, `..`); called on BOTH feature_name and entity_name BEFORE any mkdir, so invalid input never reaches the filesystem. ASVS V5 Input Validation. |
| T-01-02b | Tampering | code injection via Dart identifier interpolation | mitigate | Same regex restricts names to `[a-z0-9_]` — no Dart syntax characters or shell metacharacters survive interpolation. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib `re`/`sys` only (01-RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `grep -q 'def validate_name' fclean.py` succeeds
- `grep -q '^import re' fclean.py` succeeds
- `grep -q 'No state management files generated' fclean.py` succeeds
- `python3 -m pytest tests/test_utils.py -x -q` exits 0
</verification>

<success_criteria>
- `fclean --features ../evil` prints a validation error to stderr and exits 1 with no directories created
- `fclean --features auth:user` validates both `auth` and `user`
- Running fclean without `--state` prints the exact DX-02 notice once
</success_criteria>

<output>
Create `.planning/phases/01-foundation-fixes/01-02-SUMMARY.md` when done
</output>
