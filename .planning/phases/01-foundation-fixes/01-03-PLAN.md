---
phase: 01-foundation-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - fclean.py
  - tests/test_templates.py
autonomous: true
requirements:
  - CORE-03

must_haves:
  truths:
    - "The Riverpod template generates a typed StateNotifierProvider<FeatureNotifier, FeatureState> instead of the untyped Provider((ref) => null)"
    - "The generated Riverpod file defines a FeatureState class and a FeatureNotifier extends StateNotifier<FeatureState> class"
    - "Generated Riverpod class names are PascalCase (UserProfileNotifier, UserProfileState) for snake_case feature names"
  artifacts:
    - path: "fclean.py"
      provides: "Rewritten get_riverpod_templates producing typed StateNotifierProvider stub"
      contains: "StateNotifierProvider"
    - path: "tests/test_templates.py"
      provides: "Unit test asserting typed Riverpod template and absence of the old untyped form"
      contains: "def test_riverpod_typed"
  key_links:
    - from: "get_riverpod_templates"
      to: "to_pascal_case"
      via: "name = to_pascal_case(feature) for class names"
      pattern: "to_pascal_case\\(feature\\)"
---

<objective>
Fix CORE-03. The current `get_riverpod_templates()` emits `final {feature}Provider = Provider((ref) => null);` — Dart infers `Provider<Null>`, which is unusable as a real state container. Rewrite the function body to emit a typed `StateNotifierProvider<{Name}Notifier, {Name}State>` plus the companion `{Name}State` and `{Name}Notifier extends StateNotifier<{Name}State>` classes, using `to_pascal_case(feature)` for class names.

Purpose: Generated Riverpod code must compile without type errors and give the developer a concrete typed starting point.
Output: Rewritten `get_riverpod_templates()` in `fclean.py`; `tests/test_templates.py` asserting the typed stub.

Depends on 01-01 (uses `to_pascal_case`, which 01-01 defines) and 01-02 (edits the same file — fclean.py — and sits in the established wave order).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md
@.planning/phases/01-foundation-fixes/01-PATTERNS.md
@.planning/phases/01-foundation-fixes/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite get_riverpod_templates to emit a typed StateNotifierProvider stub</name>
  <files>fclean.py</files>
  <read_first>
    - fclean.py (current get_riverpod_templates around lines 45-51; get_bloc_templates lines 9-28 as the structural model for signature and return shape; confirm to_pascal_case is present from 01-01)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (Pattern 4: Fixed Riverpod Template — full target body; Pitfall 5: provider variable name stays snake_case and is OUT OF SCOPE; Open Question 1: ROADMAP locks StateNotifierProvider)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (get_riverpod_templates replacement section — exact target body and the template function convention)
    - .planning/phases/01-foundation-fixes/01-01-SUMMARY.md (confirm to_pascal_case is importable and how it was applied)
  </read_first>
  <behavior>
    - get_riverpod_templates("user_profile") returns a dict whose single value contains "StateNotifierProvider<", "UserProfileNotifier", "UserProfileState"
    - That value does NOT contain "Provider((ref) => null)"
    - The value imports 'package:flutter_riverpod/flutter_riverpod.dart'
  </behavior>
  <action>
    Replace the entire body of `get_riverpod_templates(feature)`. Set `name = to_pascal_case(feature)` as the first line (matching the get_bloc_templates convention). Return a dict with the single key `f"presentation/providers/{feature}_provider.dart"` whose value is a Dart source string containing, in order:
    - the import line `import 'package:flutter_riverpod/flutter_riverpod.dart';`
    - `class {name}State {{}}`
    - `class {name}Notifier extends StateNotifier<{name}State> {{` with a constructor `{name}Notifier() : super({name}State());` and closing brace
    - `final {feature}Provider = StateNotifierProvider<{name}Notifier, {name}State>((ref) {{ return {name}Notifier(); }});`

    Use the exact f-string form given in 01-PATTERNS.md "Full replacement body for get_riverpod_templates()". Keep the provider variable name as `{feature}Provider` (snake_case) — per RESEARCH.md Pitfall 5 and Open Question 2, the camelCase variable-name lint is a deferred cosmetic issue and is explicitly OUT OF SCOPE for CORE-03. Do not change any other template function.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'.'); from fclean import get_riverpod_templates as g; c=list(g('user_profile').values())[0]; assert 'StateNotifierProvider<' in c; assert 'UserProfileNotifier' in c; assert 'UserProfileState' in c; assert 'Provider((ref) => null)' not in c; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - fclean.py contains `StateNotifierProvider<` inside get_riverpod_templates
    - fclean.py no longer contains `Provider((ref) => null)`
    - Behavior: `python3 -c "import sys; sys.path.insert(0,'.'); from fclean import get_riverpod_templates as g; print(list(g('user_profile').values())[0])"` prints a stub containing `UserProfileNotifier`, `UserProfileState`, and `StateNotifierProvider<UserProfileNotifier, UserProfileState>`
  </acceptance_criteria>
  <done>get_riverpod_templates emits a typed StateNotifierProvider stub with FeatureState and FeatureNotifier classes; the untyped Provider((ref) => null) form is gone; class names are PascalCase.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_templates.py asserting the typed Riverpod stub</name>
  <files>tests/test_templates.py</files>
  <read_first>
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (Wave 0 stub for tests/test_templates.py — exact test_riverpod_typed body and import note)
    - fclean.py (confirm get_riverpod_templates is module-level and importable)
  </read_first>
  <action>
    Create `tests/test_templates.py` with the sys.path.insert(0, ".") import header and the comment `# NOTE: import path changes after Phase 2 package restructure`. Import `get_riverpod_templates` from fclean. Define `test_riverpod_typed`: call `get_riverpod_templates("user_profile")`, take the single value, and assert `"StateNotifierProvider<"`, `"UserProfileNotifier"`, `"UserProfileState"` are present and `"Provider((ref) => null)"` is absent.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_templates.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_templates.py contains `def test_riverpod_typed`
    - `python3 -m pytest tests/test_templates.py -x -q` exits 0
  </acceptance_criteria>
  <done>tests/test_templates.py exists and passes; the CORE-03 typed-template fix is locked by an automated assertion that also forbids the old untyped form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg -> generated Dart class/provider name | feature name is interpolated into Riverpod class names and a provider variable name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | feature name interpolated into Riverpod template identifiers | accept | Name content is already validated upstream by validate_name (plan 01-02) to `[a-z0-9_]` before create_feature dispatches to any template function; the template adds no new untrusted input. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure string template change (01-RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `grep -q 'StateNotifierProvider' fclean.py` succeeds
- `grep -c 'Provider((ref) => null)' fclean.py` returns 0
- `python3 -m pytest tests/test_templates.py -x -q` exits 0
</verification>

<success_criteria>
- `fclean --features user_profile --state riverpod` generates a `_provider.dart` containing a typed `StateNotifierProvider<UserProfileNotifier, UserProfileState>` plus the two companion classes
- The generated file no longer contains `Provider((ref) => null)`
</success_criteria>

<output>
Create `.planning/phases/01-foundation-fixes/01-03-SUMMARY.md` when done
</output>
